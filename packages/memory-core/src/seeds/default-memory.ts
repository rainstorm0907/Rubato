/**
 * Default memory block content constants.
 *
 * Persona and human seeds used to live here. They are gone: those files are no
 * longer part of a fresh memory repo, and their replacement (soul/user) is a
 * later design. This module now only exports the empty default-label list so
 * existing imports keep compiling while they are unwound.
 */

export const DEFAULT_MEMORY_BLOCK_LABELS = [] as const
