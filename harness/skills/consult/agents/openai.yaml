interface:
  display_name: "Consult"
  short_description: "Get and verify a focused ChatGPT web second opinion"
  default_prompt: "Use $consult to prepare a focused packet, obtain a ChatGPT web second opinion through agbrowse, and verify the advice locally."
policy:
  allow_implicit_invocation: true
