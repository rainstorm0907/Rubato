diff --git a/dist/modes/interactive/components/assistant-message.d.ts b/dist/modes/interactive/components/assistant-message.d.ts
index bcd492cd..699163f0 100644
--- a/dist/modes/interactive/components/assistant-message.d.ts
+++ b/dist/modes/interactive/components/assistant-message.d.ts
@@ -15,9 +15,12 @@ export declare class AssistantMessageComponent extends Container {
     private hasToolCalls;
     private expanded;
     private isStreaming;
+    private thinkingExpanded;
+    private readonly thinkingAction;
     constructor(message?: AssistantMessage, hideThinkingBlock?: boolean, markdownTheme?: MarkdownTheme, hiddenThinkingLabel?: string, outputPad?: number, markdownTransformers?: readonly MarkdownTransformer[]);
     invalidate(): void;
     setHideThinkingBlock(hide: boolean): void;
+    dispose(): void;
     setHiddenThinkingLabel(label: string): void;
     setExpanded(expanded: boolean): void;
     setOutputPad(padding: number): void;
diff --git a/dist/modes/interactive/components/assistant-message.js b/dist/modes/interactive/components/assistant-message.js
index 638080c4..947324d8 100644
--- a/dist/modes/interactive/components/assistant-message.js
+++ b/dist/modes/interactive/components/assistant-message.js
@@ -1,5 +1,6 @@
-import { Container, Markdown, Spacer, Text } from "@earendil-works/pi-tui";
+import { Container, hyperlink, Markdown, Spacer, Text } from "@earendil-works/pi-tui";
 import { getMarkdownTheme, theme } from "../theme/theme.js";
+import { registerInternalAction } from "../internal-actions.js";
 import { createAssistantRenderDescriptors } from "./assistant-render-descriptors.js";
 import { createMarkdownTransform } from "./markdown-transform.js";
 import { createBoundedRenderSignature } from "./render-signature.js";
@@ -16,7 +17,12 @@ export class AssistantMessageComponent extends Container {
         this.hasToolCalls = false;
         this.expanded = false;
         this.isStreaming = false;
+        this.thinkingExpanded = false;
         this.hideThinkingBlock = hideThinkingBlock;
+        this.thinkingAction = registerInternalAction(() => {
+            this.thinkingExpanded = !this.thinkingExpanded;
+            this.refreshContent();
+        });
         this.markdownTheme = markdownTheme;
         this.hiddenThinkingLabel = hiddenThinkingLabel;
         this.outputPad = outputPad;
@@ -39,6 +45,10 @@ export class AssistantMessageComponent extends Container {
         this.hideThinkingBlock = hide;
         this.refreshContent();
     }
+    dispose() {
+        this.thinkingAction.dispose();
+        super.dispose();
+    }
     setHiddenThinkingLabel(label) {
         if (this.hiddenThinkingLabel === label)
             return;
@@ -88,7 +98,7 @@ export class AssistantMessageComponent extends Container {
         const descriptors = createAssistantRenderDescriptors(message, {
             expanded: this.expanded,
             hiddenThinkingLabel: this.hiddenThinkingLabel,
-            hideThinkingBlock: this.hideThinkingBlock,
+            hideThinkingBlock: this.hideThinkingBlock && !this.thinkingExpanded,
             hasToolCalls: this.hasToolCalls,
         });
         this.reconcileRenderDescriptors(descriptors);
@@ -133,6 +143,7 @@ export class AssistantMessageComponent extends Container {
                     transform: createMarkdownTransform("assistant-thinking", this.isStreaming, this.markdownTransformers),
                 });
             case "thinking-label":
+                return new Text(hyperlink(descriptor.text, this.thinkingAction.url), this.outputPad, 0);
             case "error-text":
                 return new Text(descriptor.text, this.outputPad, 0);
             case "provider-native-summary":
diff --git a/dist/modes/interactive/components/tool-execution.d.ts b/dist/modes/interactive/components/tool-execution.d.ts
index 5a144f64..27ebd6c1 100644
--- a/dist/modes/interactive/components/tool-execution.d.ts
+++ b/dist/modes/interactive/components/tool-execution.d.ts
@@ -14,6 +14,8 @@
     private readonly images;
     private readonly grokRow;
     private readonly presentation;
+    private readonly toggleAction;
+    private readonly alwaysExpanded;
     private args;
     private expanded;
     private showImages;
@@ -40,6 +42,7 @@
     setShowImages(show: boolean): void;
     setImageWidthCells(width: number): void;
     invalidate(): void;
+    get isExpanded(): boolean;
     render(width: number): string[];
     private updateDisplay;
     private createRenderState;
diff --git a/dist/modes/interactive/components/tool-execution.js b/dist/modes/interactive/components/tool-execution.js
index ff051a21..3f0a3a19 100644
--- a/dist/modes/interactive/components/tool-execution.js
+++ b/dist/modes/interactive/components/tool-execution.js
@@ -1,7 +1,9 @@
-import { Container, Spacer } from "@earendil-works/pi-tui";
+import { Container, hyperlink, Spacer } from "@earendil-works/pi-tui";
+import { stripAnsi } from "../../../utils/ansi.js";
 import { getTextOutput as getRenderedTextOutput } from "../../../core/tools/render-utils.js";
 import { GrokToolRow } from "../grok/tool-row.js";
 import { theme } from "../theme/theme.js";
+import { registerInternalAction } from "../internal-actions.js";
 import { readToolProgress } from "../tool-progress.js";
 import { keyHint } from "./keybinding-hints.js";
 import { createBoundedRenderSignature } from "./render-signature.js";
@@ -10,6 +12,9 @@
 import { ToolExecutionRenderer } from "./tool-execution-renderer.js";
 const PENDING_RENDER_FRAME_INTERVAL_MS = 80;
 const FALLBACK_PREVIEW_LINES = 10;
+/** Tools whose result IS the content: collapsing them leaves zero information, so they always render fully expanded. */
+const ALWAYS_EXPANDED_TOOLS = new Set(["todo", "task"]);
+const COLLAPSED_ERROR_TAIL_MAX_LENGTH = 160;
 function collapseFallbackResult(result, showImages, expanded) {
     if (!result || expanded)
         return result;
@@ -24,6 +29,18 @@
         `${theme.fg("muted", `\n... (${remaining} more lines,`)} ${keyHint("app.tools.expand", "to expand")}${theme.fg("muted", ")")}`;
     return { ...result, content: [{ type: "text", text }] };
 }
+function collapseToolLines(lines, isError) {
+    const nonEmpty = lines.filter((line) => stripAnsi(line).trim().length > 0);
+    const first = nonEmpty[0] ?? lines[0] ?? "";
+    if (!isError)
+        return [first];
+    const tailSource = nonEmpty[nonEmpty.length - 1] ?? "";
+    let tail = tailSource;
+    if (tail.length > COLLAPSED_ERROR_TAIL_MAX_LENGTH) {
+        tail = `...${tail.slice(-(COLLAPSED_ERROR_TAIL_MAX_LENGTH - 3))}`;
+    }
+    return [first, theme.fg("error", tail)];
+}
 export class ToolExecutionComponent extends Container {
     constructor(toolName, toolCallId, args, options = {}, toolDefinition, ui, cwd, presentation = "classic") {
         super();
@@ -37,6 +54,11 @@
         this.imageWidthCells = options.imageWidthCells ?? 60;
         this.ui = ui;
         this.presentation = presentation;
+        this.alwaysExpanded = ALWAYS_EXPANDED_TOOLS.has(toolName);
+        this.toggleAction = registerInternalAction(() => {
+            this.setExpanded(!this.expanded);
+            this.ui.requestRender();
+        });
         const initialState = this.createRenderState();
         if (this.presentation === "grok") {
             this.grokRow = new GrokToolRow({
@@ -99,6 +121,7 @@
     }
     dispose() {
         this.stopAnimation();
+        this.toggleAction.dispose();
         super.dispose();
     }
     setExpanded(expanded) {
@@ -119,6 +142,9 @@
         this.lastDisplaySignature = undefined;
         this.updateDisplay();
     }
+    get isExpanded() {
+        return this.alwaysExpanded || this.expanded;
+    }
     render(width) {
         if (this.presentation === "grok")
             return super.render(width);
@@ -132,13 +158,20 @@
         if (renderer.hasRendererDefinition && renderer.renderShell === "self") {
             const contentLines = renderer.render(width);
             const imageLines = images.render(width);
-            if (contentLines.length === 0 && imageLines.length === 0)
+            if (contentLines.length === 0 && imageLines.length === 0) {
+                this.cachedWidth = width;
+                this.cachedSignature = signature;
+                this.cachedLines = [];
                 return [];
+            }
             lines = contentLines.length > 0 ? ["", ...contentLines, ...imageLines] : imageLines;
         }
         else {
             lines = super.render(width);
         }
+        if (!this.isExpanded) {
+            lines = collapseToolLines(lines, this.result?.isError === true).map((line) => hyperlink(line, this.toggleAction.url));
+        }
         this.cachedWidth = width;
         this.cachedSignature = signature;
         this.cachedLines = [...lines];
@@ -176,7 +209,7 @@
             executionStarted: this.executionStarted,
             argsComplete: this.argsComplete,
             isPartial: this.isPartial,
-            expanded: this.expanded,
+            expanded: this.isExpanded,
             showImages: this.showImages,
             spinnerFrame: this.spinnerFrame,
             result: this.result,
@@ -264,4 +297,4 @@
         this.cachedWidth = undefined;
     }
 }
-//# sourceMappingURL=tool-execution.js.map
\ No newline at end of file
+//# sourceMappingURL=tool-execution.js.map
diff --git a/dist/modes/interactive/interactive-mode.js b/dist/modes/interactive/interactive-mode.js
index 8cd3c563..c58e2de8 100644
--- a/dist/modes/interactive/interactive-mode.js
+++ b/dist/modes/interactive/interactive-mode.js
@@ -86,6 +86,7 @@ import { expandEditorSubmission, expandSubmittedText, transferEditorContent } fr
 import { formatExtensionErrorHeadline, sanitizeTuiErrorMessage } from "./extension-error-format.js";
 import { editFileInExternalEditor, editInExternalEditor } from "./external-editor.js";
 import { GrokChrome } from "./grok/chrome.js";
+import { dispatchInternalAction } from "./internal-actions.js";
 import { restoreInteractiveStderr, takeOverInteractiveStderr } from "./interactive-stderr-guard.js";
 import { applyKeybindingsFileEdit, seedKeybindingsFile } from "./keybindings-command.js";
 import { refreshModelCatalogs } from "./model-catalog-refresh.js";
@@ -415,7 +416,10 @@ export function createInteractiveTui(options) {
         return new TuiAltScreen(terminal, options.showHardwareCursor, options.logDirectory, {
             searchMatchStyle: (text) => theme.underline(styleSearchMatch(text)),
             searchCurrentMatchStyle: (text) => theme.bold(theme.inverse(styleSearchMatch(text))),
-            openUrl: openBrowser,
+            openUrl: (url) => {
+                if (!dispatchInternalAction(url))
+                    openBrowser(url);
+            },
             onRightClickPaste: options.onRightClickPaste,
             copySelection: async (text) => {
                 try {
diff --git a/dist/modes/interactive/internal-actions.d.ts b/dist/modes/interactive/internal-actions.d.ts
new file mode 100644
index 00000000..0d544c07
--- /dev/null
+++ b/dist/modes/interactive/internal-actions.d.ts
@@ -0,0 +1,6 @@
+export interface InternalActionRegistration {
+    url: string;
+    dispose(): void;
+}
+export declare function registerInternalAction(action: () => void): InternalActionRegistration;
+export declare function dispatchInternalAction(url: string): boolean;
diff --git a/dist/modes/interactive/internal-actions.js b/dist/modes/interactive/internal-actions.js
new file mode 100644
index 00000000..72eefbb1
--- /dev/null
+++ b/dist/modes/interactive/internal-actions.js
@@ -0,0 +1,21 @@
+const INTERNAL_ACTION_PREFIX = "senpi-action:";
+const actions = new Map();
+let nextActionId = 0;
+
+export function registerInternalAction(action) {
+    const url = `${INTERNAL_ACTION_PREFIX}${++nextActionId}`;
+    actions.set(url, action);
+    return {
+        url,
+        dispose() {
+            actions.delete(url);
+        },
+    };
+}
+
+export function dispatchInternalAction(url) {
+    if (!url.startsWith(INTERNAL_ACTION_PREFIX))
+        return false;
+    actions.get(url)?.();
+    return true;
+}
diff --git a/dist/core/extensions/builtin/compaction/speculative.js b/dist/core/extensions/builtin/compaction/speculative.js
index 608424ca..b853605b 100644
--- a/dist/core/extensions/builtin/compaction/speculative.js
+++ b/dist/core/extensions/builtin/compaction/speculative.js
@@ -88,33 +88,6 @@
     }
     return headroom;
 }
-/**
- * Reasoning override for summarization requests. Compaction must be fast: a
- * summarization request that inherits the provider's default reasoning mode
- * burns its latency (and output budget) on invisible thinking before emitting
- * the summary. Disable or minimize reasoning per wire family; adapters ignore
- * options their provider does not support. Mirrors how OpenAI Codex keeps its
- * compaction turn cheap.
- */
-function summarizationReasoningOptions(model) {
-    if (!model.reasoning)
-        return {};
-    if (model.api === "anthropic-messages")
-        return { thinkingEnabled: false };
-    const reasoningEffort = ["low", "medium", "high"].find((level) => model.thinkingLevelMap?.[level] !== null);
-    if (!reasoningEffort)
-        return {};
-    switch (model.api) {
-        case "openai-responses":
-        case "openai-codex-responses":
-        case "azure-openai-responses":
-            return { reasoningEffort, reasoningSummary: null };
-        case "openai-completions":
-            return { reasoningEffort };
-        default:
-            return {};
-    }
-}
 function getSummaryText(message) {
     const content = Array.isArray(message.content)
         ? message.content
@@ -183,7 +156,6 @@
             },
             maxTokens: summaryMaxTokens(options.snapshot.model, options.snapshot.contextWindow),
             signal: requestController.signal,
-            ...summarizationReasoningOptions(options.snapshot.model),
         });
         await consumeStreamWithIdleTimeout(responseStream, {
             idleTimeoutMs: DEFAULT_SUMMARIZATION_IDLE_TIMEOUT_MS,
